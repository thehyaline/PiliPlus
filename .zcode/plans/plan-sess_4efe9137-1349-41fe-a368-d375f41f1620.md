## 合并上游 PiliPlus 2.1.1.2 到本地 main

### 背景（已确认）
- 本仓库是 https://github.com/bggRGjQaUbCoE/PiliPlus 的 fork，`upstream` 远程已配置
- 本地 main 已包含上游 2.1.1（merge commit 78c0a9589），merge-base 为 58c73b53f
- 上游 2.1.1.2 = commit `edd60d6`（"fix custom shutdown wheel"，含 2.1.1.1 的 `2c87a32` "fix describe semantics label"），共 2 个提交

### 上游改动内容（4 个文件，已核实）
1. `lib/common/widgets/svg/level_icon.dart` — 语义标签加 `textDirection = .ltr`（+3/−1）
2. `lib/common/widgets/svg/play_icon.dart` — 同上
3. `lib/plugin/pl_player/view/widgets.dart` — `_RenderVideoTime.describeSemanticsConfiguration` 中 `config.label = ...` 改为 cascade 加 `..textDirection = .ltr`（+3/−1）
4. `lib/services/shutdown_timer_service.dart` — 注释掉 Cupertino picker 的 `looping: true`，分钟项数 25→60（+2/−2）

### 冲突预判
- 文件 1、2、4：本地自 2.1.1 后未改动 → 预期干净合并
- 文件 3：本地有 +57/−9 改动（播放器控制栏宽屏适配，位于 `_RenderVideoTime` 的其他区域），但上游修改的 `config.label = 'position:$_position\nduration:$_duration';` 行本地原样保留（widgets.dart:504）→ 预期自动合并；若出现冲突，保留本地改动并应用上游的 `..textDirection = .ltr` 修改即可

### 执行步骤
1. `git fetch upstream`（获取 2.1.1.2 / edd60d6）
2. 验证提交链：`git log --oneline 58c73b53f..edd60d6` 应正好是 2c87a32 和 edd60d6 两个提交
3. `git merge edd60d6`，merge commit 信息按惯例写 "Merge: 合并上游 2.1.1.2 (edd60d6)"
4. 如有冲突按上述方案解决，逐一确认无遗漏
5. 跟随一个提交（按惯例命名"合并上游2.1.1.2版本"），将 pubspec.yaml 版本号更新为 `2.1.1.2-<合并后sha>+5245`（用户已确认）
6. 不构建、不推送，合并结果由用户自行构建验证；最后汇报合并摘要

### 备注
- 上游 2.1.1.x 未改 pubspec.yaml / 依赖，pubspec.lock 无需更新
- 不执行 `git push`（如需推送请用户确认）