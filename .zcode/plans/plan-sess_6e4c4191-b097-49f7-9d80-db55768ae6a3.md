把检查更新相关的地址从原作者仓库 `bggRGjQaUbCoE/PiliPlus` 替换为你 fork 的 `thehyaline/PiliPlus`（来自 git remote origin，如与实际不符请指出）。

## 改动内容（共 2 处，均为 const 字符串替换）

1. **`lib/http/api.dart:413-414`** — 更新检查请求地址：
   ```dart
   // 改前
   static const String latestApp =
       'https://api.github.com/repos/bggRGjQaUbCoE/PiliPlus/releases';
   // 改后
   static const String latestApp =
       'https://api.github.com/repos/thehyaline/PiliPlus/releases';
   ```

2. **`lib/common/constants.dart:3`** — 页面跳转链接（更新弹窗、关于页、issues 等）：
   ```dart
   // 改前
   static const sourceCodeUrl = 'https://github.com/bggRGjQaUbCoE/PiliPlus';
   // 改后
   static const sourceCodeUrl = 'https://github.com/thehyaline/PiliPlus';
   ```

## 不做改动但需要你了解的事项

- README 里的 shields.io 徽章也指向原仓库，只影响展示，不影响应用功能，可选改。
- 更新判断逻辑是比对 release 的 `created_at` 与编译时间 `pili.time`（非版本号）。你自己构建时用现有的 `build_windows.bat` / `lib/scripts/build.ps1` 生成 `pili_release.json` 即可，无需额外修改代码。
- 你的 fork 上需要创建 Release（带 zip/exe 等资产），检查更新和下载按钮才能正常工作；未认证的 GitHub Releases API 有 60 次/小时/IP 限流，每次启动自动检查会消耗一次。
- 改动后如需验证，可运行 `flutter analyze` 确认无编译问题（不修改代码逻辑，风险极低）。